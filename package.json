{
  "name": "chikko-share",
  "version": "1.0.0",
  "description": "WebRTC P2P File Sharing",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npm run build && npm run copy-static",
    "start": "cd backend && npm start"
  }
}
